# YOLO Detection 2026 - Dependencies
# Install: pip install -r requirements.txt

ultralytics>=8.3.0        # YOLOv11/v10/v8 inference
numpy>=1.24.0,<2.0.0
opencv-python-headless>=4.8.0
Pillow>=10.0.0
scipy>=1.7.0
onnxruntime>=1.10.0       # Required for face recognition module
requests>=2.25.0          # Required for model downloads in deploy scripts
faiss-cpu>=1.14.2         # Vector search database for visitor profiles

# GPU-accelerated onnxruntime (replaces base onnxruntime):
# AMD ROCm:  pip install onnxruntime-rocm  (do NOT install onnxruntime alongside)
# NVIDIA:    pip install onnxruntime-gpu
